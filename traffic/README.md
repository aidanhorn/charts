# Traffic

Deferred, not built — lowest-priority pillar. The one free public source found, [TomTom's Traffic Index](https://www.tomtom.com/traffic-index/), is client-side (JavaScript) rendered with no accessible data/API endpoint short of browser-based network inspection.

`index.html` exists (a "coming soon" placeholder) but is deliberately unlinked from every page's nav — reachable only by direct URL.
